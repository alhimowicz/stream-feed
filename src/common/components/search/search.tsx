import { Box } from '@mui/system';
import React, { ReactElement, useState } from 'react';

export const Search = (): ReactElement => {
    const [toggle, setToggle] = useState<'input' | 'icon'>('icon');
    
    return (
        <Box>

        </Box>
    );
};
